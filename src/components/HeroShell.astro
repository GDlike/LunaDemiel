---
const {
  image = "/portada.webp",
  alt = "Portada",
  logoSrc = "/logo.png",
  logoAlt = "Logo Borja & Susan",
  variant = "default", // "default" | "index"

  // Móvil: menos “zoom” real (scale < 1), Desktop: el toque pro (scale-105)
  coverScale = "scale-[0.92] md:scale-105",

  // Móvil: cover (sin bandas) con encuadre un pelín hacia abajo.
  // Ajusta el 58-68 si quieres más cielo o más playa.
  coverPos = "object-cover object-[50%_62%] md:object-cover md:object-center",

  coverExtra = "blur-[1px] opacity-80",
} = Astro.props;

const imgClass = `w-full h-full ${coverPos} ${coverScale} ${coverExtra}`;
---

<header class="relative w-full h-[90vh] overflow-hidden">
  <!-- Fondo FULL BLEED -->
  <div class="absolute inset-0 z-0 select-none pointer-events-none">
    <div class="absolute inset-0 left-1/2 w-screen -translate-x-1/2 bg-[#0a0a0a]">
      <img src={image} alt={alt} class={imgClass} />

      <!-- overlays -->
      <div class="absolute inset-0 bg-gradient-to-b from-black/20 via-black/60 to-[#0a0a0a]"></div>
      <div class="absolute bottom-0 left-0 w-full h-64 bg-gradient-to-t from-[#0a0a0a] via-[#0a0a0a] to-transparent"></div>

      <!-- FX opcionales -->
      <div class="absolute inset-0">
        <slot name="fx" />
      </div>
    </div>
  </div>

  <!-- Contenido -->
  <div class="relative z-10 w-full h-full flex flex-col">
    <!-- Este wrapper centra verticalmente el conjunto (logo + slots) como en PC -->
    <div class="flex-1 flex flex-col items-center justify-center px-4 pt-24 md:pt-24">
      <!-- Logo -->
      <img
        src={logoSrc}
        alt={logoAlt}
        class="w-full max-w-[200px] md:max-w-[300px] drop-shadow-2xl rounded-2xl"
      />

      <!-- Contenido del hero -->
      <div class={`w-full ${variant === "index" ? "mt-6 md:mt-8" : "mt-8 md:mt-10"}`}>
        <div class="max-w-4xl mx-auto flex flex-col items-center text-center">
          <slot />
        </div>
      </div>
    </div>
  </div>
</header>


